{
    "name": "worker",
    "private": true,
    "scripts": {
        "dev": "go run cmd/worker/main.go",
        "build": "go build -o bin/worker cmd/worker/main.go",
        "lint": "go vet ./...",
        "test": "go test ./..."
    }
}